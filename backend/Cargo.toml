[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.8.5", features = ["small_rng"] }


[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
rayon = "1.10.0"
